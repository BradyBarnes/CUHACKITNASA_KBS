# 1️⃣ Allow scripts to run
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned

# 2️⃣ Install Node.js LTS (using winget)
winget install OpenJS.NodeJS.LTS -e --id OpenJS.NodeJS.LTS

# 3️⃣ Install Git (using winget)
winget install Git.Git -e --id Git.Git

# 4️⃣ Verify installations
node -v
npm -v
git --version

# 5️⃣ Clone the repository
git clone https://github.com/bradybarnes/CUHACKITNASA_KBS.git
cd CUHACKITNASA_KBS

# 6️⃣ Install backend dependencies
npm install

# 7️⃣ Move into the React client folder
cd client

# 8️⃣ Install frontend dependencies
npm install

# 9️⃣ Build React frontend for production
npm run build

# 10️⃣ Go back to root folder
cd ..

# 11️⃣ Start Express backend
npm start
